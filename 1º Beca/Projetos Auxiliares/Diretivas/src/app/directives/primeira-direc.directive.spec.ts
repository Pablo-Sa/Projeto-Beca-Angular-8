import { PrimeiraDirecDirective } from './primeira-direc.directive';

describe('PrimeiraDirecDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimeiraDirecDirective();
    expect(directive).toBeTruthy();
  });
});
