import { DirectiveCustomDirective } from './directive-custom.directive';

describe('DirectiveCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveCustomDirective();
    expect(directive).toBeTruthy();
  });
});
